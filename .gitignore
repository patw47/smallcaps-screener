.env
.env.local

# Config locale (edge non versionné — seul local.example.yml est committé)
config/local.yml
screener_data.json
__pycache__/
*.pyc
node_modules/
.DS_Store

# Python test/tool caches
.pytest_cache/
.ruff_cache/

# Local Claude Code guidance (canonical docs are in README.md / docs/)
CLAUDE.md

# Sprint handoff summaries (live in Obsidian, not in the repo)
.sprint-*-summary.md

# Machine-specific symlinks into the Obsidian vault (remapped per epic/sprint)
.epic.md
.sprint.md
.conventions

# Local Claude Code tooling — generated per project, never committed
.claude/worktrees/
.claude/settings.local.json
.claude/memory/
.claude/agents/

# Données runtime du tracker (snapshots extraits du volume docker pour le test d'invariance)
data/
